{% set name = "marquez-python" %}
{% set version = "0.33.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/marquez-python-{{ version }}.tar.gz
  sha256: 43ed4eb100d42e72a356d587599974b381ab627e7d7a39c709adca1f92357d7b

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - requests
    - six
    - pyrfc3339
    - deprecation

test:
  imports:
    - examples
    - marquez_client
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/marquez-python/
  summary: Marquez Python Client
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706
